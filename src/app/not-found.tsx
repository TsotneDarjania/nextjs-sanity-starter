import { Layout } from "@/ui";

export default function NotFoundPage() {
  return (
    <Layout className="flex justify-center items-center">
      <h1 className=" font-semibold">Sorry but Page Not Found </h1>
    </Layout>
  );
}
